version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: node:20
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    command: npm run dev
    environment:
      - NODE_ENV=development
